{
  "name": "relai",
  "private": true,
  "workspaces": [
    "packages/*",
    "shared/*"
  ],
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:ui": "pnpm --parallel --filter @getrelai/api --filter @getrelai/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run --workspace=vitest.workspace.ts",
    "demo": "pnpm --filter @getrelai/orchestrator exec tsx ../../scripts/demo.ts",
    "start-worker": "node_modules/.pnpm/node_modules/.bin/tsx scripts/start-worker.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}