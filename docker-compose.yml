services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: relai
      POSTGRES_PASSWORD: relai
      POSTGRES_DB: relai
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
